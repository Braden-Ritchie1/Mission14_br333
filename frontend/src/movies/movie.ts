//this file is called in the Movies.tsx to define data types
export type Movie = {
  movieId: number;
  title: string;
  category: string;
  director: string;
  year: number;
  rating: string;
  edited: string;
  lentTo: string;
  notes: string;
};
